def most_frequent_integer(numbers:list) -> int:
    frequency = dict()
    most_ocurred = (None, 0) # (int, ocurr)

    for i in numbers:
        if i in frequency.keys():
            frequency[i] += 1
        else:
            frequency[i] = 1

        if most_ocurred[1] < frequency[i]:
            most_ocurred = (i,frequency[i])

    return most_ocurred[1]

if __name__ == '__main__':
    list_of_numbers = [1,2,3,3,3,4,4,5]
    print(most_frequent_integer(list_of_numbers)) # return 3
